[package]
name = "fabryk-mcp-graph"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Graph query MCP tools for Fabryk"

[features]
default = []
graph-rkyv-cache = ["fabryk-graph/graph-rkyv-cache"]

[dependencies]
fabryk-core = { version = "0.5.0", path = "../fabryk-core" }
fabryk-graph = { version = "0.5.0", path = "../fabryk-graph" }
fabryk-mcp-core = { version = "0.5.0", path = "../fabryk-mcp-core" }

# Async
tokio = { workspace = true }
async-trait = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Schema
schemars = { workspace = true }

# Graph (for types)
petgraph = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tokio-test = { workspace = true }
